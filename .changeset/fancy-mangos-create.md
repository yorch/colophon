---

---

Test-only: deflake `MermaidDiagram › re-renders the diagram when the app theme
changes`. No published package changes behaviour, so this changeset releases
nothing — it exists to record that deliberately rather than skip the check.
